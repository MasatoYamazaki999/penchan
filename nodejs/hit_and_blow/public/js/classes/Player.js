class Player {
  constructor({ username, history, target, battle, ans }) {
    this.username = username
    this.history = history
    this.target = target
    this.battle = battle
    this.ans = ans
  }
}
